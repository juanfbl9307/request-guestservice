/* import { RequestResponseStub } from '../../test/stubs/requestStub';

export const RequestRepository = jest.fn().mockReturnValue({
  findAll: jest.fn().mockResolvedValue([RequestResponseStub()]),
  findById: jest.fn().mockResolvedValue(RequestResponseStub()),
});
 */
